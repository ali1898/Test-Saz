{
  "name": "qa",
  "version": "0.1.0",
  "description": "Cypress test project generator with AI assistance — POM, BDD, Allure, CI/CD",
  "workspaces": [
    "packages/core",
    "packages/cli"
  ],
  "bin": {
    "qa": "./packages/cli/dist/index.js"
  },
  "files": [
    "packages/cli/dist",
    "packages/core/dist",
    "packages/*/package.json",
    "qa.cmd",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:core": "npm run build --workspace @qa-test-generator/core",
    "build:cli": "npm run build --workspace @qa-test-generator/cli",
    "dev:cli": "npm run dev --workspace @qa-test-generator/cli",
    "unlink": "npm unlink -g qa @qa-test-generator/cli @qa-test-generator/core",
    "qa": "node packages/cli/dist/index.js",
    "prepack": "npm run build",
    "clean": "rimraf packages/*/dist packages/*/node_modules"
  },
  "devDependencies": {
    "marked": "^18.0.5",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
